<div class="action-plan" id="action-plan-<%= action_plan.id %>">
  <ul class="action-plan-description">
    <li><h3><%= action_plan.description %></h3></li>
    <li><%= link_to "Show Goals", "#", class:"show-goals", data: {action_plan_id: action_plan.id }%></li>
    <li><%= link_to "Show All", "#", class:"show-goals-steps", data: {action_plan_id: action_plan.id }%></li>
    <li><%= link_to "Delete", action_plan, remote: true, method: :delete, class: "delete-action-plan" %></li>
  </ul>

  <div class="action-plan-goals hidden">
    <div class="goals">
      <% action_plan.goals.each do |goal| %>
        <%= render partial: 'shared/goal', locals: {goal: goal, new_step: new_step} %>
      <% end %>
    </div>
    <%= link_to "Add Goal", "#", id: "add-goal", data: {action_plan_id: action_plan.id } %>
    <%= form_for new_goal, remote: true, html: {class:"hidden"}  do |f| %>
      <%= f.text_field :description, placeholder: "Description"%>
      <%= f.hidden_field :action_plan_id, value: action_plan.id %>
      <%= f.submit "Add Goal" %>
    <% end %>
  </div>
</div>
